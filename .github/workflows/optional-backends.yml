name: optional-backends

on:
  workflow_dispatch:
  schedule:
    - cron: "17 6 * * 1"
  pull_request:
    paths:
      - "generaldia/quantum/**"
      - "generaldia/electronic_structure/**"
      - "examples/quantum/**"
      - "examples/pyscf/**"
      - "pyproject.toml"

jobs:
  quantum:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install -e ".[dev,quantum]"
      - run: pytest -m optional

  pyscf:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install -e ".[dev,pyscf]"
      - run: pytest tests/test_electronic_structure.py
      - run: python examples/pyscf/01_h2_rhf.py
